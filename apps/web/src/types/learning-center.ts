export type LearningTrack = {
  name: string;
  modules: number;
  hours: number;
  progress: number;
};

export type LearningAchievement = { title: string; description: string };
export type LearningCertificate = { name: string; date: string };
