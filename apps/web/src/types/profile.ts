export type ProfileSummary = {
  name: string;
  initials: string;
  title: string;
  email: string;
  score: string;
  solved: string;
  rank: string;
};

export type SkillMastery = { label: string; value: number };
